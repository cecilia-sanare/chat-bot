{
  "name": "@flarie/core",
  "source": "index.ts",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rm -rf dist/ && bun build --target node --outdir ./dist --production .",
    "types": "tsc -p ./tsconfig.json --emitDeclarationOnly"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chalk": "^2.2.4",
    "concurrently": "^9.2.1",
    "tsx": "^4.21.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ribbon-studios/flarie.git"
  },
  "license": "AGPL-3.0-only",
  "dependencies": {
    "chalk": "^5.6.2"
  }
}
